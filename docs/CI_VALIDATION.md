# CI validation

This file is intentionally tiny and exists to force a clean pull-request CI run for the v0.2 implementation.

The CI workflow is the authoritative automated check for the repository's current test and demo commands.
